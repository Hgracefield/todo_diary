import 'package:flutter/material.dart';

class DiaryWriteView extends StatelessWidget {
  const DiaryWriteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DiaryWriteView'),
      ),
    );
  }
}
