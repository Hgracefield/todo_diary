import 'package:flutter/material.dart';

class DiaryCalendarView extends StatelessWidget {
  const DiaryCalendarView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DiaryCalendarView'),
      ),
    );
  }
}
