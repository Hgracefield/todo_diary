import 'package:flutter/material.dart';

class ScheduleFormView extends StatelessWidget {
  const ScheduleFormView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ScheduleFormView'),
      ),
    );
  }
}
